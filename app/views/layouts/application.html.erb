<!DOCTYPE html>
<html>
  <head>
    <title>AlbumReview <%= yield(:title) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= favicon_link_tag %> 
    <%= action_cable_meta_tag %>

    <meta property="og:title" content="Album Review <%= yield(:title) %>">
    <meta property="og:description" content="Share reviews of your favorite albums with family and friends">
    <meta property="og:image" content="http://www.seaboltdesign.com/record.jpg">
    <meta property="og:url" content="http://www.albumreview.herokuapp.com">
    <meta name="twitter:card" content="summary_large_image">
  </head>

  <body>
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          <a class="navbar-brand" href="/"><span style="color: white">Album</span><span class='light'>Review</span></a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
              <li><%= link_to "Home", albums_path %></li>
              <li><%= link_to "Add Album", new_album_path %> </li>
            <% if user_signed_in? %>
              <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
            <% else %> 
              <li><%= link_to "Log in", new_user_session_path %></li>
            <% end %> 
          </ul>
          <form class="navbar-form navbar-left">
            <div class="form-group">
            <%= text_field_tag :search, params[:search], placeholder: 'Search', class: "form-control form-control nav_search" %>
            </div>
            <%= submit_tag "Search", class: "btn btn-nav" %>
          </form>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
    <div class="navbar_placeholder">
    </div>
  	<div class="container-fluid">
    	<%= yield %>
    </div>
  </body>
</html>
